{application, myserver,
    [
        {description, "myserver"}
        ,{vsn, "1"}
        ,{modules, [myserver]}
        ,{registered, [myserver_sup]}
        ,{applications, [kernel, stdlib, sasl]}
        ,{mod, {myserver_app, []}}
        ,{start_phases, []}
    ]
}.
